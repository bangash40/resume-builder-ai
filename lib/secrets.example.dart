// Template for lib/secrets.dart, which is git-ignored so the real key is never
// committed. Copy this file to lib/secrets.dart and paste your key.
// Get a free key at https://aistudio.google.com (no billing required).
const String geminiApiKey = 'YOUR_GEMINI_API_KEY';
